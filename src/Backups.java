public class Backups {


    // THE OLD Velocity hit
      /*
    public Velocity hit(Point collisionPoint, Velocity currentVelocity) {
        double newDx = currentVelocity.getDx();
        double newDy = currentVelocity.getDy();

        // collision at the corners will change the 2 components of the speed (the 2 if below are taken)
        if ((collisionPoint.getX() >= rectangle.getDownLeft().getX()) && (collisionPoint.getX() <= rectangle.getDownRight().getX())) {
            newDy = (-1) * newDy;
        }

        if ((collisionPoint.getY() >= rectangle.getDownLeft().getY()) && (collisionPoint.getY() <= rectangle.getUpperLeft().getY())) {
            newDx = (-1) * newDx;
        }

        Velocity newVelocity = new Velocity(newDx, newDy);

        return newVelocity;
        }  // end of hit
*/
}
